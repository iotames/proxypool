# proxypool 隧道代理工具 – 产品需求文档（架构师版）

## 1. 项目定位与我的角色

我是本项目的架构师，负责 proxypool 的整体技术方案设计，确保系统在满足原始需求的基础上，具备良好的可扩展性、可维护性和开发效率。  
proxypool 是一个面向爬虫开发者的轻量级隧道代理工具。它从 Clash 配置中提取节点，自动构建代理池，并在本地暴露一个固定端口，为所有出站请求随机分配出口 IP，从而规避反爬虫策略。

## 2. 原始需求（不可变）

以下为项目最初的业务需求，所有设计必须完整实现，不得修改：

> 读取clash的 .yaml 配置文件，运行多个代理节点组成代理池，但只暴露一个端口作为隧道代理，随机切换网络请求的IP。如：http://127.0.0.1:1080  
> 1. 目的：给爬虫应用使用防止被反爬虫，执行命令 `proxypool --port=1080 --conf=xxx.yml` 开启服务。  
> 2. 选择可用节点：测试谷歌首页的访问速度，默认延迟230以内的作为可用节点，组成代理池。  
> 3. 使用说明：把详细的注释和使用说明都写到源码文件中。  
> 4. 先规划后执行：优先使用交互式对话，用户确认需求后执行。  
> 5. 自行请求测试网页，验证不同请求的客户的IP是否随机变化，自己完善产品。

## 3. 设计目标与核心原则

- **一键启动**：`proxypool --port=1080 --conf=xxx.yml` 即可运行，零额外配置。
- **随机 IP 隧道**：每个通过代理端口的请求，都从可用池中随机选取一个节点转发，实现 IP 轮换。
- **自动节点管理**：基于谷歌连通性测试筛选节点，失败自动剔除，定时尝试恢复。
- **安全边界**：Clash 配置文件为只读，绝不修改；proxypool 自身配置通过 `.env` 文件管理，与 Clash 完全独立。
- **前端可管可看**：提供一个轻量 Web 管理界面，用于查看代理池状态、编辑配置、浏览节点信息。
- **开发友好**：后端与前端解耦，前端可借助 Mock 服务独立运行开发，降低环境依赖。

## 4. 系统整体架构

proxypool 采用 **Go 单体服务 + 内嵌 Web 前端** 的架构，核心模块如下：

```
┌─────────────────────────────────┐
│          Web 管理界面            │
│  (Web Components + Vue/React)   │
└──────────────┬──────────────────┘
               │ HTTP API
┌──────────────▼──────────────────┐
│          Go 后端服务             │
│  ┌──────────────────────────┐   │
│  │  Clash 解析器             │   │
│  │  (YAML → 节点模型)        │   │
│  ├──────────────────────────┤   │
│  │  节点测试 & 池管理        │   │
│  │  (并发测试 → 池排序)      │   │
│  ├──────────────────────────┤   │
│  │  隧道代理服务             │   │
│  │  (HTTP/SOCKS5 自动协商)   │   │
│  ├──────────────────────────┤   │
│  │  健康检查与恢复           │   │
│  ├──────────────────────────┤   │
│  │  配置管理 (三级覆盖)      │   │
│  ├──────────────────────────┤   │
│  │  Web API 接口层          │   │
│  └──────────────────────────┘   │
└─────────────────────────────────┘
```

- **后端**：标准 Go 项目，依赖 `gopkg.in/yaml.v3` 解析 YAML，其余使用标准库实现（网络代理、HTTP API 等）。
- **前端**：采用 **标准原生架构**，主骨架基于 Web Components 构建，业务组件可使用 Vue 或 React 自由接入（通过 veaury 等工具），做到技术栈无关、高内聚低耦合。

## 5. 后端功能模块详解

### 5.1 Clash 配置解析（只读）
- 解析本地 YAML 中的 `proxies` 字段，仅提取类型为 `ss` 和 `trojan` 的节点。
- 支持 `proxy-providers` 远程订阅（type=http），自动拉取并解析其中的节点，支持 `ss://` 和 `trojan://` URI。
- **边界**：永不对 Clash 配置文件做任何写入操作。

### 5.2 节点可用性测试与代理池构建
- 启动时并发测试所有节点的可用性，通过节点请求 `https://www.google.com`，记录延迟。
- 默认延迟阈值 230ms（可通过配置调整），延迟 ≤ 该值的节点加入代理池。
- 代理池内节点按延迟升序排列，支持通过 `POOL_MAX_SIZE` 限制最大保留数量。

### 5.3 隧道代理服务
- 监听指定端口（默认 `127.0.0.1`，端口由 `--port` 指定）。
- 自动识别并支持三种代理协议：
  - HTTP CONNECT 隧道（供浏览器/常规 HTTP 客户端）
  - SOCKS5 协议
  - 普通 HTTP 代理请求（非 CONNECT）
- 每个新连接从代理池中**随机**选取一个节点，建立双向数据转发通道。

### 5.4 健康检查与动态维护
- 转发请求时若节点连接失败，立即将该节点标记为不可用并从池中移除。
- 后台定时任务（默认 60 秒）重新测试已剔除的节点，成功则重新加入池中。
- 健康检查间隔、测试目标 URL 均可配置。

### 5.5 配置管理（easyconf 三级覆盖模式）
proxypool 自身运行配置独立于 Clash，通过以下三级机制加载，优先级由低到高：
1. `.env` 文件（持久化存储）
2. 系统环境变量
3. 命令行参数

可配置项包括：

| 配置项 | 环境变量 | 默认值 | 说明 |
|--------|----------|--------|------|
| Google 测试超时 | `GOOGLE_TIMEOUT` | 230 (ms) | 节点延迟筛选阈值 |
| 测试目标 URL | `GOOGLE_TEST_URL` | `https://www.google.com` | 用于连通性测试 |
| 健康检查间隔 | `HEALTH_INTERVAL` | 60 (s) | 剔除节点后重测间隔 |
| 池最大节点数 | `POOL_MAX_SIZE` | 0 (不限制) | 代理池容量上限 |
| 监听地址 | `BIND_ADDRESS` | `127.0.0.1` | 代理服务绑定 IP |

### 5.6 Web API 接口
后端内嵌 HTTP API 服务，供前端消费，接口清单见第 7 节。

## 6. 前端架构设计

### 6.1 设计理念：标准原生架构
前端整体遵循“企业应用中台前端”的标准原生架构思想，核心特点：
- **技术栈无关**：主框架基于 **Web Components** 构建，提供路由、导航、布局等基础能力，不依赖 Vue/React 等具体框架。
- **组件化与微前端支持**：业务页面组件可使用任意技术栈开发，通过类似 `veaury` 的方案混合集成，新业务模块可以微前端形式独立部署、动态加载。
- **逻辑与 UI 分离**：数据获取与状态管理使用 **TanStack Query（或 Refine）** 封装，UI 层仅负责渲染，支持无头组件模式。
- **动态导航与路由**：菜单结构和路由表通过统一 API 接口动态获取，方便后期按角色定制（当前版只需管理员视图）。
- **开发 Mock 支持**：所有 `/api/*` 接口均有 Mock 实现，前端可在无后端环境下独立启动、调试。

### 6.2 页面与功能

#### 仪表盘
- 服务运行状态指示
- 代理池概况：总节点数、可用数、不可用数、平均延迟

#### 配置管理（编辑 .env）
- 以表单形式展示所有可配置项，附带中文说明和默认值
- 支持保存至 `.env`、一键重置为默认值
- 明确提示：修改后的配置需重启服务方可生效

#### 节点列表
- 表格展示所有节点：名称、类型、地址、端口、延迟、状态
- 状态筛选（全部/可用/不可用）、延迟排序
- 单节点“手动测试”按钮，实时更新状态
- 状态颜色标识：绿色（可用）、红色（不可用）、灰色（待测试）

### 6.3 技术选型建议
| 项目 | 选型 | 说明 |
|------|------|------|
| 基础骨架 | Web Components + 自定义路由 | 保持框架无关，可对接任意组件 |
| 业务组件 | Vue 3 或 React（视团队偏好） | 通过 veaury 等方式混入 Web Components 容器 |
| 数据请求 | TanStack Query（React）或 Refine | 统一缓存、异常处理、加载状态 |
| 样式方案 | CSS Variables + 无头组件样式 | 保持灵活，不强依赖某 UI 库 |
| 打包工具 | Vite | 支持多框架混合开发 |

## 7. 前后端 API 接口定义

| 方法 | 路径 | 说明 | 返回示例 |
|------|------|------|----------|
| GET | `/api/status` | 代理池总览 | `{ running, totalNodes, availableNodes, avgLatency, recentLogs[] }` |
| GET | `/api/config` | 当前 .env 配置 | `{ GOOGLE_TIMEOUT: 230, ... }` |
| PUT | `/api/config` | 更新 .env 配置 | `{ success: true }` |
| GET | `/api/nodes` | 节点列表（含状态） | `[ { name, type, host, port, latency, status } ]` |
| POST | `/api/nodes/{name}/test` | 测试单个节点 | `{ name, latency, status }` |
| GET | `/api/logs` | 最近请求日志 | `[ { time, target, node, latency } ]` |

所有接口均遵循 RESTful 风格，返回 JSON。

## 8. 开发计划

### Phase 1：后端核心（5-7 天）
1. `.env` 配置加载与三级覆盖机制  
2. Clash YAML 解析（本地 + 远程）  
3. 节点并发连通性测试 + 代理池构建  
4. HTTP/SOCKS5 隧道服务与随机轮换  
5. 健康检查循环  
6. 自测验证：多次请求 `https://httpbin.org/ip`，确认 IP 随机变化

### Phase 2：Web API 与前端（3-5 天）
1. 实现后端 Web API 接口  
2. 前端搭建 Web Components 骨架 + 动态路由  
3. 开发仪表盘、配置管理、节点列表页面（结合 Mock 数据）  
4. 前后端联调，样式打磨

### Phase 3：完善与交付（1-2 天）
1. 编写 README 及使用文档  
2. 异常场景处理（网络中断、配置错误等）  
3. 测试多款 Clash 配置兼容性  
4. 压力测试与性能调优

## 9. 工程规范

- **语言**：Go 后端，前端组件可使用 JS/TS + Vue/React。
- **注释**：所有导出符号须有中文注释，关键逻辑附行内注释；文档、commit message 统一使用中文。
- **配置管理**：`.env` 文件由 proxypool 维护，不与 Clash 配置混合。
- **代码质量**：Go 使用类型推断，提交前检查无 AI 辅助标记（如 Co-Authored-By）。
- **依赖安装**：优先使用国内镜像源。

以上需求文档从架构师视角完整定义了 proxypool 的形态，既满足了原始需求，又兼顾了工程化和可扩展性，可作为开发团队的执行基线。